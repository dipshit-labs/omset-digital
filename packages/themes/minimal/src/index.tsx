import type { ThemeProps } from "@repo/types";

export function ThemeRenderer(_props: ThemeProps) {
  return (
    <div data-theme="minimal">
      <p>Minimal theme — stub. Full implementation in ticket #18.</p>
    </div>
  );
}
